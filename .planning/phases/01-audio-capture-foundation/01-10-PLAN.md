---
phase: 01-audio-capture-foundation
plan: 10
type: execute
wave: 2
depends_on: ["09"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - src/metamemory/widgets/main_widget.py
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Widget has a non-interactive drag surface that can start dragging"
    - "Clicking empty widget areas is consumed by the widget (no click-through to underlying apps)"
    - "Dragging starts only from the empty/drag surface; clicking buttons/lobes still triggers their actions and does not drag"
  artifacts:
    - path: "src/metamemory/widgets/main_widget.py"
      provides: "Hit-testable drag surface item behind controls"
      contains: "DragSurfaceItem"
    - path: "src/metamemory/widgets/main_widget.py"
      provides: "Drag-start transition (movement threshold) sets is_dragging=True"
      contains: "is_dragging = True"
  key_links:
    - from: "src/metamemory/widgets/main_widget.py:MeetAndReadWidget._create_components()"
      to: "QGraphicsScene"
      via: "add full-rect background item behind controls"
      pattern: "addItem"
    - from: "src/metamemory/widgets/main_widget.py:MeetAndReadWidget.mouseMoveEvent()"
      to: "MeetAndReadWidget.is_dragging"
      via: "movement threshold to initiate drag"
      pattern: "is_dragging"
---

<objective>
Fix widget drag and click-through by adding a hit-testable background drag surface and restoring the drag-start transition.

Purpose: UAT expects the widget can be dragged from empty/non-interactive areas; currently there is no hit-testable empty area (click-through) and dragging never starts because `is_dragging` is never set True.
Output: Drag surface + correct click-vs-drag state machine.
</objective>

<execution_context>
@C:\Users\david.keymel\.config\opencode/get-shit-done/workflows/execute-plan.md
@C:\Users\david.keymel\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-audio-capture-foundation/01-UAT.md
@.planning/debug/uat-01-widget-drag-surface.md

# Gap being closed
- truth: "Widget can be dragged from empty/non-interactive areas"
- status: failed
- root_cause: "Fully transparent view with no full-rect hit-testable surface, so empty pixels click-through; drag logic never sets is_dragging=True so dragging cannot start"

# Relevant code
- src/metamemory/widgets/main_widget.py:34-50 - translucent/transparent window setup
- src/metamemory/widgets/main_widget.py:91-126 - scene items created (no background item)
- src/metamemory/widgets/main_widget.py:148-193 - mouse handlers (is_dragging never set True)
</context>

<tasks>

<task type="auto">
  <name>Add a near-invisible full-rect drag surface item behind all controls</name>
  <files>src/metamemory/widgets/main_widget.py</files>
  <action>
Create a hit-testable background item so the widget has an "empty area" that captures input (prevents click-through) and can be used to initiate dragging.

Implementation approach (keep it in this file):
1) Add a small helper class near the top of the file (above MeetAndReadWidget), e.g. `class DragSurfaceItem(QGraphicsRectItem):`.
- It should:
  - Initialize with a rect covering the scene (same size as widget: 200x120).
  - Use `setPen(Qt.PenStyle.NoPen)`.
  - Use a brush with alpha ~1 (e.g. `QColor(0, 0, 0, 1)`) so it is effectively invisible but still painted.
  - Set `setZValue(-1000)` so it stays behind everything.
  - Accept left mouse button events (`setAcceptedMouseButtons(Qt.MouseButton.LeftButton)`).
  - Do NOT implement any click behavior; it just exists to be hit-testable.

2) In `MeetAndReadWidget._create_components()`:
- Create `self.drag_surface = DragSurfaceItem(...)` and add it to the scene BEFORE adding buttons/lobes.

3) Ensure the rect matches the current scene rect:
- After `self._scene.setSceneRect(0, 0, 200, 120)` in `__init__`, set the drag surface rect to `self._scene.sceneRect()`.

Avoid:
- Do NOT change window flags or add any OS-specific click-through hacks.
- Do NOT make the drag surface visibly opaque.
  </action>
  <verify>python -c "from pathlib import Path; p=Path('src/metamemory/widgets/main_widget.py'); s=p.read_text(encoding='utf-8'); print('DragSurfaceItem' in s and 'setZValue' in s)"</verify>
  <done>Widget scene contains a full-rect, near-invisible background item so empty areas are hit-testable</done>
</task>

<task type="auto">
  <name>Fix drag state machine: start dragging after movement threshold only when press began on drag surface</name>
  <files>src/metamemory/widgets/main_widget.py</files>
  <action>
Restore dragging by making `mouseMoveEvent` transition into dragging when the cursor moves enough AND the press started on the background drag surface.

Update `MeetAndReadWidget` event handling:

1) Track whether the press began on the drag surface:
- In `mousePressEvent`, after recording `drag_start_pos` and `widget_start_pos`, determine which item is under the cursor:
  - Use `self.itemAt(event.position().toPoint())` (or an equivalent QGraphicsView API) to get the topmost item.
  - Set a boolean field like `self._press_on_drag_surface = (item is self.drag_surface)`.
- Continue to call `super().mousePressEvent(event)` so child items still receive clicks.

2) Start drag on move:
- In `mouseMoveEvent`, when NOT already dragging:
  - If left button is held AND `self._press_on_drag_surface` is True:
    - Compute movement from `drag_start_pos` using `manhattanLength()`.
    - If movement exceeds a threshold (reuse existing `5` pixels), set `self.is_dragging = True`.
  - Once `is_dragging` becomes True, run the existing move-window logic (move, undock, update docked state) and `event.accept()`.
- If `_press_on_drag_surface` is False, never set `is_dragging=True` (prevents dragging from buttons/lobes).

3) Consume empty-area clicks:
- In `mouseReleaseEvent`, if `_press_on_drag_surface` is True and the movement is below click threshold, do NOT call `super().mouseReleaseEvent(event)`; instead `event.accept()` so the click is consumed and does not click-through.

4) Ensure drag finalization remains:
- When dragging ends (mouseRelease after movement beyond threshold), set `is_dragging=False`, run `_check_snap_to_edge()`, and accept the event.

Avoid:
- Do NOT change click behavior for RecordButtonItem/ToggleLobeItem/SettingsLobeItem.
- Do NOT allow dragging to start from interactive items.
  </action>
  <verify>python -c "from pathlib import Path; s=Path('src/metamemory/widgets/main_widget.py').read_text(encoding='utf-8'); print('_press_on_drag_surface' in s and 'is_dragging = True' in s)"</verify>
  <done>Dragging can start from the background surface after threshold; empty-area clicks are consumed; interactive item clicks still work</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Widget drag surface (no click-through) + restored drag-start transition</what-built>
  <how-to-verify>
  1) Launch widget (repo `src/` layout):
     - Preferred (robust): `python -c "import runpy,sys; sys.path.insert(0,'src'); runpy.run_module('metamemory.main', run_name='__main__')"`
     - If you already run with `PYTHONPATH=src`: `python -m metamemory.main`
  2) Put another app/window behind it (e.g. a browser) and focus the other window
  3) Click on a visually empty area of the widget (not on any button/lobe)
     - Expected: the underlying app does NOT receive the click (no click-through)
  4) Click and drag from that empty area
     - Expected: widget moves with the cursor
  5) Click the record button once
     - Expected: record toggles (no dragging)
  6) Click mic/system lobes once
     - Expected: they toggle (no dragging)
  7) Drag the widget near a screen edge and release
     - Expected: snap-to-edge behavior still works

  8) Update tracking (Phase 1 completion):
     - Update `.planning/ROADMAP.md` (Phase 1 section) to include and mark as completed:
       - `[x] 01-09-PLAN.md - Fix CLI fake duration cap via max_frames + regression test`
       - `[x] 01-10-PLAN.md - Fix widget drag surface + click-through`
     - Update `.planning/STATE.md` to reflect Phase 1 is complete again (Plan: 10 of 10, Phase 1 ✅ / 100%).
  </how-to-verify>
  <resume-signal>Type "approved" if behavior matches expected, or describe what failed</resume-signal>
</task>

</tasks>

<verification>
UAT Test 4 should now pass:
- "Click on empty widget area and drag - widget SHOULD drag to new position"
- "Everything that is not a button should NOT click-through"
</verification>

<success_criteria>
- User can drag the widget from empty/non-interactive areas
- Empty-area clicks are consumed by the widget (no click-through)
- Clicking record/toggle/settings items still triggers actions and does not initiate dragging
- Snap-to-edge behavior remains intact after dragging
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-capture-foundation/01-10-SUMMARY.md`
</output>
