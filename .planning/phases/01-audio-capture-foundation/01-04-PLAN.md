---
phase: 01-audio-capture-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/metamemory/recording/__init__.py
  - src/metamemory/recording/controller.py
  - src/metamemory/widgets/main_widget.py
  - src/metamemory/main.py
autonomous: false

must_haves:
  truths:
    - "User can select audio source(s) before recording (mic/system/both) and start/stop from the widget."
    - "If no source is selected, recording does not start and the UI shows a non-blocking error indicator."
    - "On startup, the app offers to recover any leftover partial recordings."
    - "A 30+ minute recording completes without dropouts or corruption (manual verification)."
  artifacts:
    - path: "src/metamemory/recording/controller.py"
      provides: "UI-friendly controller around AudioSession"
    - path: "src/metamemory/widgets/main_widget.py"
      provides: "Widget wires record button + source toggles to recording controller"
    - path: "src/metamemory/main.py"
      provides: "Startup recovery prompt + app entry"
  key_links:
    - from: "src/metamemory/widgets/main_widget.py"
      to: "src/metamemory/recording/controller.py"
      via: "start/stop calls"
      pattern: "RecordingController"
    - from: "src/metamemory/main.py"
      to: "src/metamemory/audio/storage/recovery.py"
      via: "recover_part_files"
      pattern: "recover"
---

<objective>
Wire the widget UI to the recording pipeline, add startup recovery UX, and verify real device capture quality and long-run stability.

Purpose: Complete Phase 1 user-facing flow and validate against success criteria.
Output: Working widget-driven recording (mic/system/both) + recovery prompt + manual verification checklist.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-capture-foundation/01-CONTEXT.md
@.planning/phases/01-audio-capture-foundation/01-RESEARCH.md
@src/metamemory/widgets/main_widget.py
@.planning/phases/01-audio-capture-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RecordingController and wire widget start/stop</name>
  <files>
src/metamemory/recording/__init__.py
src/metamemory/recording/controller.py
src/metamemory/widgets/main_widget.py
  </files>
  <action>
- Create `RecordingController` as a thin wrapper around `AudioSession`:
  - `start(selected_sources: set[str])` and `stop() -> Path`.
  - Provide non-blocking behavior for UI (run session stop/finalize on a worker thread).
  - Surface errors (no devices, permission errors) as a string/state the widget can render.
- Update `src/metamemory/widgets/main_widget.py`:
  - Use mic/system lobe active states as the pre-recording selection (AUD-04).
  - On record-button click:
    - If neither mic nor system selected: do not start; set a visible, non-modal error indicator.
    - Else: start session; update button visual state to recording.
  - On stop: call controller stop; when done, transition to idle and log/print the file path.
- Keep the UI feedback visual-only (per context): no modal dialogs for normal start/stop.
  </action>
  <verify>python -m pytest -q</verify>
  <done>
- Widget start/stop calls the controller and does not freeze while finalizing.
- No-source-selected prevents recording and shows a visible indicator.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add startup crash recovery prompt</name>
  <files>src/metamemory/main.py</files>
  <action>
- On app startup (before showing the widget), scan recordings dir for leftover `*.pcm.part` files via `metamemory.audio.storage.recovery`.
- If any exist:
  - Show a single `QMessageBox` asking whether to recover them now.
  - If user accepts: run recovery and report how many WAVs were produced.
  - If user declines: leave files untouched.
- Do not block the UI thread for long recovery: if multiple large files, do recovery in a worker thread and show progress minimally (text is fine).
  </action>
  <verify>python -c "from metamemory.audio.storage.recovery import find_part_files; print('ok')"</verify>
  <done>
- App offers recovery when partial recordings are present.
- Declining recovery leaves `.pcm.part` intact.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Widget-driven mic/system/both recording + recovery prompt</what-built>
  <how-to-verify>
1. Install deps: `pip install -r requirements.txt`
2. Run app: `python -m metamemory.main`
3. Mic-only:
   - Enable mic lobe, disable system lobe
   - Click record, speak for ~10 seconds, click stop
   - Open the saved WAV in a media player and confirm clear mic audio
4. System-only:
   - Disable mic lobe, enable system lobe
   - Play a known audio source (YouTube/music)
   - Record ~10 seconds, stop, confirm system audio is present
5. Both:
   - Enable both lobes
   - Record a short segment while speaking + playing system audio
   - Confirm both are audible in the saved WAV
6. Crash recovery simulation:
   - Start recording, then force-kill the process
   - Relaunch app and confirm recovery prompt appears; accept and confirm a recovered WAV exists
7. Long-run stability:
   - Record for 30+ minutes (any source)
   - Stop and confirm WAV plays through without corruption/dropouts
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest -q` passes
- Manual verification checklist approved
</verification>

<success_criteria>
- Phase 1 success criteria satisfied for mic/system/both capture, disk streaming, recovery, and 30+ minute stability.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-capture-foundation/01-04-SUMMARY.md`
</output>
