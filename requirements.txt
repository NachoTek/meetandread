# meetandread requirements

# GUI Framework
PyQt6>=6.4.0

# Audio Processing
sounddevice>=0.4.6  # WASAPI via PortAudio
numpy>=1.24.0       # Frame conversion + mixing
soxr>=0.3.0         # Resampling
comtypes>=1.2.0     # Windows COM interface for WASAPI loopback

# AI/ML - Whisper
faster-whisper>=1.1.0  # 4x faster than openai-whisper, CTranslate2 optimized
torch>=2.0.0           # Required for VAC (Voice Activity Controller)
torchaudio>=2.0.0      # Audio processing for VAC

# Speaker Diarization
# pyannote.audio will be added for voice signatures

# System utilities
psutil>=5.9.0

# Development
pytest>=7.0.0
black>=22.0.0
mypy>=1.0.0