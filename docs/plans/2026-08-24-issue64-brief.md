> *This was generated by AI during triage.*

## Agent Brief

**Category:** bug
**Summary:** Verify the native-crash class from #64 is healed on main, then remove the CI carve-out that ignores `tests/test_settings_history.py`

**Current behavior:**

Re-verified during triage on main @ `38ae997` (post-#69 merge `ffbc5ae`), Windows venv (Python 3.11.16, PyQt6 6.11.0 / Qt 6.11.0):

- `tests/test_settings_history.py` alone: **174/174 passed** (142s) — no crash.
- Original 3-module repro (`test_settings_history.py` + `test_retranscribe.py` + `test_transcript_management.py`, single process): **253/253 passed twice** (152s, 157s) — the reported access-violation-at-test-37 no longer reproduces.
- Likely cause landed in #69 (`ffbc5ae`): the `FloatingTranscriptPanel._refresh_after_retranscribe` `AttributeError` escaping into the Qt event loop (exact trigger this issue predicted), plus the status-pill QSS stray-brace fix that had Qt logging a stylesheet parse error per Library row.
- CI's pytest step still carries `--ignore=tests/test_settings_history.py` — the file never runs in CI.
- The **full** single-process Windows suite has **not** been re-run on current main; the last known state (pre-#69) was an access violation at ~test 54. Full run takes ~2h on this VM.

**Desired behavior:**

The full Windows suite completes cleanly in one process on main, and CI no longer skips the settings-history tests. If the full run still crashes natively, the carve-out stays and the crash stack is reported back on this issue instead.

**Key interfaces:**

- CI test workflow (nightly + push/PR) pytest invocation — the `--ignore=tests/test_settings_history.py` argument to remove. Leave every other pytest flag (`-p no:qt`, markers, `--timeout`) untouched.
- `make test-windows` (Windows venv pytest, single process) — the authoritative full-suite layer per the test-execution-topology ADR.
- Known flake, not a #64 symptom: `test_retranscribe_creates_sidecar` asserts a background thread finishes within a 5s wall-clock `Event.wait`; it timed out once in three full-speed runs during triage, passed solo and on re-run. If it fails in the full run, retry in isolation before calling it a regression.

**Acceptance criteria:**

- [ ] Full single-process Windows suite (`make test-windows`) completes on main with 0 failed and no native crash (no access violation / segfault abort). Record pass count + wall time on this issue.
- [ ] If the full run crashes natively: do NOT remove the carve-out — post the crash stack here and stop.
- [ ] `--ignore=tests/test_settings_history.py` removed from the CI test workflow; no other pytest flags changed.
- [ ] A CI run (workflow_dispatch is fine) executes the settings-history tests and passes — link the run on this issue.
- [ ] Any single-test failure in the full run is retried in isolation and the outcome reported (pass-after-retry = flake, note it; persistent = real regression, report it).

**Out of scope:**

- The WSL logic layer — WSL is decommissioned on this machine per the maintainer; all WSL/Py3.14 segfault claims in the original report and bot comments are dropped from this issue.
- Changing `-p no:qt` or any other CI pytest configuration.
- Fixing the `test_retranscribe_creates_sidecar` timing flake (candidate for its own issue).
- Revisiting the #69 fixes themselves.

---

**Triage notes:** maintainer confirmed WSL is gone (WSL claims dropped) and approved the Windows/CI-only scope. Verification evidence above; the crash-path fix arrived via #69, so this issue reduces to "verify + un-carve CI".
